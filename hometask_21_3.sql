-- 3. Написати sql запит, який виведе інформацію про вік (кількість років) та кількість користувачів, які відповідають цьому віку. Результатом виконання такого запиту має бути таблиця:
SELECT age, COUNT(age) AS users FROM users GROUP BY age